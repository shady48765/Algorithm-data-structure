//查找函数，其中，elem表示要查找的数据元素的值
int selectTable(table t, int elem){
        for (int i=0;i<t.length; i++){
                if (t.head[i]==elem {
                                return i+1;
                                }
         }
                                return -1;//如果查找失败，返回-1
}


//查找函数，其中，elem表示要查找的数据元素的值
int selectTable(table t, int elem){
    for (int i=0;i<t.length; i++){
        if (t.head[i]==elem){
            return i+1;
            }
        }
        return -1;//返回错误提示-1
}

// 查找函数，查找值为elem的脚标
int selcetTable (table t, int elem) {

        for  (i=0; i<t.length; i++) {
                if (t.head[i]==elem) {
                        return i+1;
                }
        } 
        return -1;//返回错误提示-1
}

    
